# `break()` command

Break from an enclosing `foreach()` or `while()` loop.

> **Syntax**
> 
> ```cmake
> break()
> ```

---

## References
 
> [cmake-official-docs](https://cmake.org/cmake/help/latest/command/break.html)