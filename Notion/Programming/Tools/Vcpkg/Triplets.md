- _Triplet_ is a standard term used in cross compiling as a way to  
    completely capture the target environment (cpu, os, compiler, runtime,  
    etc.) in a single, convenient name.
- vcpkg comes with pre-defined triplets for many common platforms and configurations. Run vcpkg help triplet to get the list available in your environment.