A database view defines how the data is presented.
Each view has several components that you can customize:
- **Layout:** This is the general structure of the view.
    
    For more info, see below.
    
- **Properties:** Show or hide properties.
- **Filters:** 
- **Sorts:** 
- **Groups:** 
## Layouts
### Table
This is the most traditional way of viewing a database. It allows you to see your dataset as rows of pages, with every property represented by a column.
![[/Untitled 2.png|Untitled 2.png]]
### **Board**
This view groups your items by property. For example, use it as a Kanban board to move an item through phases of a process.
![[Untitled 1.png]]
### **Timeline**
Use your database to plot projects on a timeline so you can visualize when they are taking place and how long you think they will take to complete.
![[/Untitled 2 2.png|Untitled 2 2.png]]
### **Calendar**
Displays your items based on their `Date` property.
![[Untitled 3.png]]
### **List**
A minimal, vertical layout of your items that lets you open them as a page with one click.
![[Untitled 4.png]]
### **Gallery**
Use your database to display images. You can edit your gallery to show images contained in the `Files & media` property or content inside each page.
![[Untitled 5.png]]
## Properties
Properties are stored in the database, a view only allows you to show or hide them.
See more [[Properties]].
## Filters
Each view has its own set of filter criteria.
Add criteria based on property values to show or hide data.
**Note:** In a shared database, any changes you make to filters and sorts in views won’t affect other people on the team until you click `Save for everyone`.
**Add an advanced filter**
You can create more specific database views and combine `AND` and `OR` logic by using filter groups. These can be nested up to three layers deep!
## Sorts
sort by a property to change the order that pages are displayed in.
Different properties sort by different logic, depending on their value type.
- Text properties such as `Name` and `Text` sort alphabetically.
- Number properties sort numerically.
- For `Select` and `Multi-select` properties, you get to define what sorting order means. Click on the property, then drag options up or down to set the sort order.
## Groups
Group your data by the values in a property.